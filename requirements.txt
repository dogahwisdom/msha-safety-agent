# Core data and modeling
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0
requests>=2.32.0

# Vector store and embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Testing (tool modules independently of the agent)
pytest>=8.0.0

# LLM provider SDK (not pinned yet).
# Tell me which provider you are using (e.g. OpenAI, Anthropic, Google Gemini,
# Azure OpenAI, Ollama/local), and I will add the matching package here.
# Examples (do not install until confirmed):
#   openai>=1.40.0
#   anthropic>=0.34.0
#   google-generativeai>=0.8.0
